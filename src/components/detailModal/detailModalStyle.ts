import styled from 'styled-components';
import { Modal } from 'antd';

const DetailModalStyle = styled(Modal)`
  img {
    height: 300px;
  }
`;

const SectionStyle = styled.section`
  border-bottom: 10px;
`;

export { DetailModalStyle, SectionStyle };
