import styled from 'styled-components';
import { Layout, Menu } from 'antd';
const { Header } = Layout;

const NavStyle = styled(Header)`
  width: 100%;
  padding: 0;
`;

const MenuStyle = styled(Menu)``;

export { NavStyle, MenuStyle };
